import VirginDumpingAuthWrapper from './auth-wrapper';

// Force this page to be dynamic
export const dynamic = 'force-dynamic';

export default function VirginDumpingPage() {
  return <VirginDumpingAuthWrapper />;
}
